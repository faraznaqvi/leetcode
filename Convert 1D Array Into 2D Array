import numpy as np
class Solution:
    def construct2DArray(self, original: List[int], m: int, n: int) -> List[List[int]]:
        try:
            x = np.array(original).reshape(m,n) 
            return x
        except:
            return []
